// Tableaux
let array = ['chien', 'chat', 'poisson']

array.push('cows')

console.log(array);


// Objets
let player = {
    id:0,
    name: "Zidane",
}
const sayName = () => {
    console.log(player.suname);
}
sayName();