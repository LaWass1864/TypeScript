// Tableaux
let array = ['chien', 'chat', 'poisson']

array.push('cows')

console.log(array);


// Objets
let player = {
    id:0,
    name: "Zidane",
}
const sayPlayerName= () => {
    console.log(player.suname);
}
sayPlayerName();

const sayName = name => {
    console.log(`bonjour ${name}`);
}

sayName()